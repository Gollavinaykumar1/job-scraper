{
  "name": "job-scraper",
  "version": "1.0.0",
  "description": "Playwright job scraper server for Railway",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "install-browsers": "npx playwright install chromium --with-deps"
  },
  "dependencies": {
    "express": "^4.18.2",
    "playwright": "^1.44.0"
  },
  "engines": {
    "node": ">=18"
  }
}
